#define _CRT_SECURE_MO_WARNINGS
#include <stdio.h>

/* 아래 코드를 스파게티 코드라고 한다. 불필요한 goto는 자제하자.*/
int main() {
	int num1; 
	
	scanf("%d", &num1);
	
    if (num ==1 ) {
		printf("1입니다.\n");
	} else if (num == 2) {
		printf("2입니다.\n");
	}
	return 0;
}
